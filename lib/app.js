// Aquí se van a alojar las microfuncionesfunction mdLinks (path){
function mdLinks (path){
    return new Promise((resolve /*cumplido*/, reject /*no cumplida*/) => {
        if (){ //path no es una ruta absoluta
        }
        else{//entraria else? creo que no
        }
    })
}